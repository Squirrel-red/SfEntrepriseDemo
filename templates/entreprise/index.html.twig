{% extends 'base.html.twig' %}

{% block title %}Liste des entreprises{% endblock %}

{% block body %}
    <h1>Liste des entreprises</h1>
    {# {{ controller_name }} #}

    {# Bonjour {{ name }} ! #}

    {# {% for element in tableau %} #}
    {# {{ element}} <br> #}
    {# {% endfor %} #}
    {#   on déclare le nom de l'atribut en ecrivant comme dans get due Etreprise.php pour cet atribut raisonSociale#}
    {#  mais dans la base des données on voit raison_sociale #}
    {% for entreprise in entreprises %}
        {# {{ entreprise.raisonSociale ~ ' (' ~ entreprise.cp ~ ' ' ~ entreprise.ville ~ ')'}} <br> #}
        {# <b>{{ entreprise.raisonSociale }}</b> {{' (' ~ entreprise.cp ~ ' ' ~ entreprise.ville ~ ')'}} <br> #}
        <b>{{ entreprise }}</b><br>
    {% endfor %}

{% endblock %}
